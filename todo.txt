merge Table.UnescapeTable into ReadTsv
CLI flags
documentation
write more tests
when to use a method of a struct type, and when to pass a struct ptr?
does passing a map as a fn arg make a new copy, and is it expensive?
is passing a struct instead of a ptr as a fn arg significantly more expensive?
